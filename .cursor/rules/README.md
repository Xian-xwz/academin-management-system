# `.cursor/rules` 说明

本目录存放 Cursor 项目级规则（`.mdc`），与 Agent 单次对话上下文无关时仍可被引用。

## 当前规则

| 文件 | 说明 |
|------|------|
| [云端与Git同步.mdc](云端与Git同步.mdc) | 公网环境路径、Git 推送、SSH 服务器拉代码、前端 build/rsync、后端重启与验收要点 |

## 维护

新增或改名规则文件时，在本表增补一行。
